language: php

dist: trusty

sudo: false

git:
    depth: 1

cache:
  directories:
    - $HOME/.composer/cache/files

matrix:
    include:
        - php: hhvm
        - php: 5.6
        - php: 5.6
          env: deps=low
        - php: 7.0
        - php: 7.1
    fast_finish: true

install:
  - make tests
