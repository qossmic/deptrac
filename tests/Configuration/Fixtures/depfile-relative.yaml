parameters:
  paths: ["./examples/Uncovered/"]

  use_relative_path_from_depfile: true

  layers:
    - name: Core
      collectors:
        - type: className
          value: Core

  ruleset:
    Core: ~
